@using System.Collections.Generic
@using System.Reflection
@using Posts;
@using DataService;

<div>
    <h3 style="color: white">CategoryMenu</h3>
    <br />
    @foreach (var post in posts)
    {
        {
            <p><button type="button">@post.Category</button></p>

            //@onclick=@(e => UriHelper.NavigateTo(post.Name))
        }
    }
</div>

@code {
    public List<PostMetadata> posts = DataService.PostsService.GetPostsMetadata();
}
